[distro]
name = "guardian"

# Guardian is intentionally minimal: one tool (execute_code), one CLI gateway,
# its own provider drivers. It does not pull in any external bundles — every
# skill it needs lives in this distro's own skills/ tree.
